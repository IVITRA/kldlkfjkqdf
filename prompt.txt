رأيت جميع الملفات. هنا التعديلات الحرجة التي يجب تطبيقها فوراً:
🔴 أولوية قصوى (Critical Fixes)
1. memory_manager.cpp - الـ RAM Guard مفقود!
المشكلة: load_blocking و decompress_worker لا يتحققان من MAX_RAM_USAGE قبل الإضافة!
الإصلاح المطلوب:
cpp
Copy
// في load_blocking()، أضف هذا قبل إضافة الخبير:
while (current_ram_usage + expert_size > MAX_RAM_USAGE && !lru_list.empty()) {
    evict_lru_if_needed(); // طرد فوري إذا كانت الذاكرة ممتلئة
}

// إذا كان الخبير الجديد أكبر من الحد الأقصى أصلاً:
if (expert_size > MAX_RAM_USAGE) {
    std::cerr << "Expert too large for RAM limit!" << std::endl;
    return nullptr;
}
2. router.h - مصفوفة ثابتة الحجم (50000) قاتلة!
cpp
Copy
// ❌ خطأ: هذا يستهلك 25.6 MB دائماً حتى لو كان لديك 100 خبير فقط!
alignas(64) float centroids_linear[50000][128]; 

// ✅ الحل: استخدم vector ديناميكي
std::vector<std::array<float, 128>> centroids_linear;
// ثم reserve(50000) عند البناء
لماذا؟ إذا شغلت النظام على جهاز بـ 1GB RAM، الـ 25MB هذه قد تكون قاتلة.
3. async_loader.cpp - الـ Threads نائمة!
المشكلة: io_loop و decomp_loop يستخدمان sleep_for (= بطء)
الإصلاح:
cpp
Copy
// ❌ بطيء:
std::this_thread::sleep_for(std::chrono::milliseconds(10));

// ✅ سريع (Busy waiting مع yield):
while (running.load()) {
    if (load_queue.dequeue(expert_id, ticket)) {
        process(expert_id);
    } else {
        std::this_thread::yield(); // سلم الـ CPU فوراً
    }
}
🟡 تحسينات متوسطة (Important Improvements)
4. router.cpp - الـ Search يحتاج Cache
أضف هذا في search_nearest:
cpp
Copy
// قبل البحث، تحقق من الـ RouterCache
RouterCache cache;
std::vector<uint32_t> cached_result;
if (cache.lookup(query_vec, cached_result)) {
    if (confidence) *confidence = 0.95f; // ثقة عالية للـ Cache
    return cached_result;
}

// ... بعد البحث ...
cache.store(query_vec, result);
5. context_protocol.h - الترجمة وهمية!
cpp
Copy
// ❌ هذا لا يفعل شيئاً:
if (from != to) {
    output.intent[i] *= 0.95f; // مجرد ضرب في 0.95!
}

// ✅ يجب إضافة مصفوفات الترجمة المدربة:
class InterlinguaTranslator {
private:
    // مصفوفات التحويل بين المجالات (يتم تدريبها مرة واحدة)
    std::array<std::array<float, 128>, 128> medical_to_legal;
    std::array<std::array<float, 128>, 128> physics_to_math;
    // ... إلخ
};
6. asm_format.h - CRC32 غير منفذ!
cpp
Copy
// يجب إضافة حساب الـ Checksum:
static uint64_t calculate_crc32(const uint8_t* data, size_t size) {
    // استخدام zlib crc32 أو implementation خاص
    return crc32_z(0L, data, size);
}
🟢 تحسينات أداء (Performance Boosts)
7. memory_manager - Prefetching مفقود
أضف:
cpp
Copy
void prefetch_experts(const std::vector<std::string>& expert_ids) {
    // تحميل في الخلفية قبل الحاجة
    for (const auto& id : expert_ids) {
        if (!get_if_available(id)) {
            request_load_async(id); // بدون انتظار
        }
    }
}
8. router - SIMD محسّن أكثر
cpp
Copy
// إضافة Prefetching للـ Centroids القادمين:
inline float avx2_distance(const float* a, const float* b) {
    // Prefetch الـ Next Cache Line
    _mm_prefetch(reinterpret_cast<const char*>(b + 128), _MM_HINT_T0);
    
    // ... باقي الكود ...
}
📋 قائمة التعديلات المطلوبة الآن
Table
الملف	السطر	التعديل
memory_manager.cpp	78, 145	إضافة RAM Guard قبل الإضافة
router.h	32	تغيير float[50000][128] إلى vector
router.cpp	15	إضافة reserve(50000)
async_loader.cpp	40, 55	استبدال sleep_for بـ yield
router.cpp	110	إضافة RouterCache lookup
asm_format.h	40	تنفيذ calculate_crc32
context_protocol.h	58	إضافة مصفوفات الترجمة الحقيقية